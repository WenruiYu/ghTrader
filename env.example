# ---------------------------------------------------------------------------
# ghTrader env.example (secrets/bootstrap only)
#
# Business/runtime thresholds have moved to Config Service (runs/control/config.db).
# Use:
#   ghtrader config migrate-env   # one-time import from legacy env keys
#   ghtrader config set ...       # ongoing config updates
#
# Legacy business env keys (for example GHTRADER_L5_VALIDATE_*) are rejected
# at startup unless:
#   GHTRADER_CONFIG_ALLOW_LEGACY_ENV=true
# ---------------------------------------------------------------------------

# ---- Core paths/bootstrap --------------------------------------------------
GHTRADER_DATA_DIR=data
GHTRADER_ARTIFACTS_DIR=artifacts
GHTRADER_RUNS_DIR=runs

# ---- Dashboard auth/bootstrap ----------------------------------------------
# Optional token for dashboard/API auth
# GHTRADER_DASHBOARD_TOKEN=replace-me

# ---- Live safety gate ------------------------------------------------------
# Must remain false for research/backtest mode.
GHTRADER_LIVE_ENABLED=false

# ---- TqSdk auth (secrets; keep local + gitignored) -------------------------
# Required for data API auth
# TQSDK_USER=your_user
# TQSDK_PASSWORD=your_password

# ---- Broker account secrets (live only; keep local + gitignored) -----------
# Primary profile (optional if using runs/control/accounts.env managed by UI)
# TQ_BROKER_ID=broker_id
# TQ_ACCOUNT_ID=account_id
# TQ_PASSWORD=account_password

# Multi-profile example (used with --account-profile p)
# GHTRADER_TQ_ACCOUNT_PROFILES=p,sim
# TQ_BROKER_ID_P=broker_id
# TQ_ACCOUNT_ID_P=account_id
# TQ_PASSWORD_P=account_password

# ---- Emergency/maintenance toggles -----------------------------------------
# Skip .env loading (rarely needed)
# GHTRADER_DISABLE_DOTENV=true
#
# Temporary bypass for legacy env enforcement during migration only.
# Remove after cutover.
# GHTRADER_CONFIG_ALLOW_LEGACY_ENV=true

